# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objective

Learn how to build RESTful APIs using the FastAPI framework. This assignment will guide you through creating endpoints, handling requests, and returning responses in a FastAPI application.

## 📝 Tasks

### 🛠️ Task 1: Setup FastAPI Project

#### Description
Set up a new FastAPI project with the necessary dependencies and folder structure.

#### Requirements
Completed program should:

- Include a `main.py` file to define the FastAPI application.
- Install FastAPI and Uvicorn as dependencies.
- Run the application locally using Uvicorn.

### 🛠️ Task 2: Create API Endpoints

#### Description
Create endpoints for a simple CRUD (Create, Read, Update, Delete) application. For example, manage a list of items.

#### Requirements
Completed program should:

- Include endpoints for creating, reading, updating, and deleting items.
- Use appropriate HTTP methods (GET, POST, PUT, DELETE).
- Validate request data using Pydantic models.

### 🛠️ Task 3: Test the API

#### Description
Test the API endpoints using tools like Postman or cURL.

#### Requirements
Completed program should:

- Verify that all endpoints work as expected.
- Handle errors gracefully and return appropriate HTTP status codes.